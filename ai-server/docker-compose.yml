version: '3.8'
services:
  ragflow:
    image: infiniflow/ragflow:latest
    ports:
      - "8080:8080"
      - "12345:12345"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: always